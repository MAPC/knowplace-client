import { renderNewline } from '../../../helpers/render-newline';
import { module, test } from 'qunit';

module('Unit | Helper | render newline');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = renderNewline(42);
  assert.ok(result);
});
