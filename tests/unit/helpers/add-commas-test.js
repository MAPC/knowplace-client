import { addCommas } from '../../../helpers/add-commas';
import { module, test } from 'qunit';

module('Unit | Helper | add commas');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = addCommas(42);
  assert.ok(result);
});
