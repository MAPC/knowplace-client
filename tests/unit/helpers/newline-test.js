import { newline } from '../../../helpers/newline';
import { module, test } from 'qunit';

module('Unit | Helper | newline');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = newline(42);
  assert.ok(result);
});
