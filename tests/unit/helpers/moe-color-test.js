import { moeColor } from '../../../helpers/moe-color';
import { module, test } from 'qunit';

module('Unit | Helper | moe color');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = moeColor(42);
  assert.ok(result);
});
