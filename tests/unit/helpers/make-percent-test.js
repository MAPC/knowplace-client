import { makePercent } from '../../../helpers/make-percent';
import { module, test } from 'qunit';

module('Unit | Helper | make percent');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = makePercent(42);
  assert.ok(result);
});
