import { downloadCsv } from '../../../helpers/download-csv';
import { module, test } from 'qunit';

module('Unit | Helper | download csv');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = downloadCsv(42);
  assert.ok(result);
});
