import Ember from 'ember';

export default Ember.Route.extend({
  // model: function(params) {
  //   return this.store.findRecord("report", params.place_id);
  // },
  // setupController: function() {
  //   alert(this.modelFor('reports.search'));
  //   this.controller.set('profiles', this.modelFor('reports.search'));
  //   // this.controller.set('model', this.modelFor('places.create.edit'));
  // }
});
